<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Account_XOR_AccountGroup</fullName>
    <active>true</active>
    <description>Deve haver ou uma conta ou um grupo de contas selecionado</description>
    <errorConditionFormula>NOT( 
  OR( 
    AND( 
      ISBLANK( Account__c ) , 
      NOT( ISBLANK( AccountGroup__c ) ) 
    ) ,
    AND( 
      ISBLANK( AccountGroup__c ) , 
      NOT( ISBLANK( Account__c ) ) 
    )   
  ) 
)</errorConditionFormula>
    <errorMessage>Deve-se selecionar uma Conta ou um Grupo de Contas, e apenas um dos dois.</errorMessage>
</ValidationRule>
